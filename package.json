{
  "name": "fim-workspace",
  "version": "0.0.1",
  "scripts": {
    "dev": "cd app && npm run tauri dev",
    "build": "cd app && npm run tauri build",
    "install-deps": "cd app && npm install"
  },
  "devDependencies": {}
}
